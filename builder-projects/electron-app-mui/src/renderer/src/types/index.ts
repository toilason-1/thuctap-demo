/**
 * Renderer Types
 *
 * Re-exports all types from the shared module for backward compatibility.
 * New code should import directly from '../../shared' instead.
 *
 * This file exists to maintain compatibility with existing imports throughout
 * the renderer codebase while centralizing type definitions in the shared module.
 */

// Re-export everything from shared
export * from '../../../shared'

// Local additions that are renderer-specific can go here
